# Single-service deploy (GCP VM): FastAPI serves this build itself, so the API
# and the WebSocket live on the same origin as the page. Empty = "use my own
# origin" — no host is baked into the bundle, so the same build works over any
# IP or hostname the VM is reached by.
# Build with:  npm run build -- --mode gcp
VITE_API_URL=
