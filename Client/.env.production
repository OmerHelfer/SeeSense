# Single-service deploy: FastAPI serves this build itself, so the API and the
# WebSocket live on the same origin as the page. Empty = "use my own origin" —
# no host is baked into the bundle, so the same build works over any IP or
# hostname the server is reached by.
VITE_API_URL=
